---
- name: install required software
  dnf:
    name: "{{ item }}"
    state: present
  with_items:
    - postfix
    - yum-cron
- name: configure Postfix service
  service:
    name: postfix
    state: started
    enabled: yes
